## 问题总结 + 改进方案

### 一、根本原因

**1. 不了解 outbox 机制**  
把「回复用户」误解为「更新 `status.json` 里的 `progress` 字段」。但 `progress` 只有用户主动发 `🍍?` 才会被读取；真正能主动推送的唯一机制是 `outbox` 数组，SKILL.md 原来的说明不够直接。

**2. 无中断响应机制**  
用户发 `🍍：xxx`，bridge 只能把事件写入 terminal stdout。Agent 如果正在执行长批次工具调用，无法感知。缺少一个轻量的「有消息来了」信号。

**3. Status JSON 信息粒度太粗**  
`"progress":"实现WP4"` 这类描述，用户发 `🍍?` 收到的回答毫无具体进度。

---

### 二、改进方案（3 条，通用）

#### 1. 中断哨兵文件

bridge 收到 `request` 事件时，写入一个哨兵文件：

```
%LOCALAPPDATA%\wechat-agent-bridge\interrupt.flag
```

Agent 在每次工具调用前用 `Test-Path` / `Path.exists()` 检查（毫秒级）。文件存在则：暂停当前原子操作 → 处理请求 → 用 outbox 回复 → 删除文件。**不存在 = 无待处理中断。**

#### 2. Outbox 主动推送

`outbox` 是唯一的主动推送通道。格式：

```json
{
  "state": "running",
  "task": "任务名",
  "outbox": [
    { "id": "stable-unique-id", "type": "received", "text": "回复内容" }
  ]
}
```

`id` 稳定唯一，跨 tick 去重。`type` 决定前缀：`received` → `🍍收到：`，`done` → `🍍完成：`。

#### 3. 结构化 Status JSON

```json
{
  "state": "running",
  "task": "任务名",
  "progress": "Step 4/10 完成，当前：Step 5",
  "detail": {
    "done": ["Step 1", "Step 2", "Step 3", "Step 4"],
    "current": "Step 5 - 具体操作描述",
    "remaining": 5
  }
}
```

`progress` 是对用户可见的一句话摘要，要具体。`detail` 用于多步骤任务，单步骤可省略。

---

### 核心结论

三条改进互补、彼此独立：哨兵文件解决「被中断时不知道」，outbox 解决「无法主动回复」，结构化 status 解决「回答没有信息量」。均已落地到代码和 SKILL 文档。